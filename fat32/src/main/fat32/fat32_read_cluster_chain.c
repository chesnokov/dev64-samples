#include <fat32/api.h>

bool fat32_read_cluster_chain(FAT32_CB_T * cb,
  UINT32 cluster, ISPROC * proc) {
/*
  UINT32 sector;
  bool result=true;

  while (result) {

  }
  sector =
    fat32_get_sector_by_cluster(cb, cb->bootSector.bpb.BPB_RootClus)
  bool result = fat32_read_sectors()
*/
  return false;
}
